Flat content container — white surface, hairline border, 8px radius. Sky-Tint header strip for metadata; subtle hover lift when `interactive`.

```jsx
<Card header="Program Performance Trends">…body…</Card>
<Card interactive>…clickable card…</Card>
<Card padded={false}><table>…</table></Card>
```

Props: `header`, `footer`, `interactive`, `padded` (default true).
