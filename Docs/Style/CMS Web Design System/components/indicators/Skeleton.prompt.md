Shimmer loading placeholder — text/block/circle.

```jsx
<Skeleton variant="text" lines={3} />
<Skeleton variant="circle" width={40} />
<Skeleton variant="block" height={160} />
```

Props: `variant`, `width`, `height`, `lines`, `radius`.
