Linear progress — determinate animated fill or indeterminate sweep.

```jsx
<ProgressBar label="Compliance rate" value={98.2} showValue />
<ProgressBar indeterminate tone="gold" />
```

Props: `value`, `max`, `label`, `showValue`, `indeterminate`, `tone` (navy/gold/success), `height`.
