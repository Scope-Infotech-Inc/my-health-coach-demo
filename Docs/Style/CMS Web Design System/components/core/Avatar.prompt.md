User avatar — image or auto-initials on a brand-tinted disc.

```jsx
<Avatar name="Jane Doe" tone="navy" />
<Avatar src="/photo.png" name="Alan Miller" size={40} ring />
```

Props: `name`, `src`, `size` (default 32), `tone` (navy/gold/sky/slate), `ring`.
