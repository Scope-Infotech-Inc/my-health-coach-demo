Labeled native select styled to match Input.

```jsx
<Select label="Sort by"><option>Newest First</option><option>Alphabetical</option></Select>
```

Props: `label`, `hint`, `required`, plus native select props + `<option>` children.
