Category / filter chip — neutral outlined pill, selectable or removable.

```jsx
<Tag selected>Transmittals</Tag>
<Tag icon="tag" onClick={...}>ICD-10 Updates</Tag>
<Tag onRemove={...}>Provider Manuals</Tag>
```

Props: `selected`, `icon`, `onRemove`, `onClick`.
