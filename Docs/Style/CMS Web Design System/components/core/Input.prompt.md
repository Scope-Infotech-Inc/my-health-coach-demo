Labeled text field — visible Lexend label, navy focus border, CMS-red error state.

```jsx
<Input label="Program name" placeholder="Enter a name" required />
<Input label="Search resources" icon="search" placeholder="Keywords, transmittal #…" />
<Input label="NPI" error="Must be 10 digits" defaultValue="123" />
```

Props: `label`, `hint`, `error`, `icon`, `required`, plus native input props.
