Numbered pagination — chevrons, navy active page, ellipsis truncation.

```jsx
<Pagination page={page} pageCount={12} onChange={setPage} />
```

Props: `page` (1-based), `pageCount`, `onChange`, `siblings`.
